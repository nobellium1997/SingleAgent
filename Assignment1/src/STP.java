public class STP
{

    //successor function

    //operator function

    //apply op
    public STPState ApplyOperator(STPState stp, SlideDir o)
    {
        switch (o){
            case Up:

            case Down:

            case Left:

            case Right:
        }

        return stp;
    }
    public enum SlideDir
    {
        Up, Down, Left, Right
    }
    //undo op
}
